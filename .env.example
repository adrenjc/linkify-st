# ShortLink 环境变量配置
# 复制此文件为 .env 并修改相应的值

# ============ 必须配置 ============

# GitHub 用户名（用于拉取 GHCR 镜像）
GITHUB_USERNAME=your_github_username

# JWT 密钥 - 生产环境必须修改为安全的随机字符串！
# 可使用以下命令生成：openssl rand -hex 32
JWT_SECRET=your_super_secure_jwt_secret_change_this

# MongoDB 连接字符串 (如果后端在宿主机运行，指向 localhost:27017)
MONGO_URI=mongodb://127.0.0.1:27017/shortlink

# Redis 配置 (如果后端在宿主机运行，指向 localhost)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ============ 可选配置 ============

# 前端端口（默认 3000）
# FRONTEND_PORT=3000

# 后端端口（默认 5000）
# BACKEND_PORT=5000
