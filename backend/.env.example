# ShortLink Backend 环境变量配置
# 复制此文件为 .env.development 或 .env.production 并填入实际值
# ⚠️ 请勿将真实密钥提交到 Git 仓库！

# ============ 基础配置 ============

# 运行环境：development 或 production
NODE_ENV=development

# 服务端口
PORT=5000

# ============ 数据库配置 ============

# MongoDB 连接字符串
# 开发环境示例：mongodb://localhost:27017/shortlink
# 生产环境示例：mongodb://localhost:27017/shortlink
MONGO_URI=mongodb://localhost:27017/shortlink

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379

# ============ 认证配置 ============

# JWT 密钥（生产环境必须使用强密钥！）
# 生成方法：openssl rand -hex 32
JWT_SECRET=your_jwt_secret_change_this

# JWT 过期时间
JWT_EXPIRE=24h

# ============ AI 功能配置（可选）============

# 阿里云 DashScope API Key（用于 AI 功能）
# 如不使用 AI 功能可留空
DASHSCOPE_API_KEY=

# AI 最大 Token 数
MAX_TOKENS=4096

# ============ 限流配置 ============

# 限流时间窗口（分钟）
RATE_LIMIT_WINDOW=15

# 每个时间窗口最大请求数
RATE_LIMIT_MAX_REQUESTS=100

# ============ SSL 证书配置 ============

# ACME 邮箱（用于 Let's Encrypt 证书通知）
ACME_EMAIL=your_email@example.com

# ============ 备份配置（可选）============

# 数据库备份目录
BACKUP_DIR=/var/backups/mongodb

# 备份保留天数
BACKUP_RETENTION_DAYS=30

# 外部备份目录（可选）
EXTERNAL_BACKUP_DIR=
